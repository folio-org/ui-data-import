{
  "name": "@folio/cataload",
  "version": "0.1.0",
  "description": "Stripes applicaiton for Data Import application.",
  "main": "src/index.js",
  "repository": "",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "start": "stripes serve",
    "build": "stripes build --output ./output",
    "test": "stripes test nightmare --run demo --show",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@folio/eslint-config-stripes": "^1.1.0",
    "@folio/stripes-cli": "^1.2.0",
    "babel-eslint": "^8.0.3",
    "eslint": "^4.8.0"
  },
  "dependencies": {
    "@folio/stripes-components": "^2.0.0",
    "prop-types": "^15.6.0",
    "react-router-dom": "^4.1.1"
  },
  "peerDependencies": {
    "react": "*"
  },
  "stripes": {
    "type": "app",
    "displayName": "ui-cataload",
    "route": "/cataload",
    "hasSettings": true,
    "okapiInterfaces": {},
    "permissionSets": [
      {
        "permissionName": "module.cataload.enabled",
        "displayName": "UI: ui-cataload module is enabled",
        "visible": true
      },
      {
        "permissionName": "settings.cataload.enabled",
        "displayName": "Settings (cataload): display list of settings pages",
        "subPermissions": [
          "settings.enabled"
        ],
        "visible": true
      }
    ]
  }
}
